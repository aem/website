import styled from 'styled-components';
import { catamaran, textColorPrimary, textSizeHuge } from 'styles';

const H2 = styled.h2`
  color: ${textColorPrimary};
  font-size: ${textSizeHuge};
`;

export default H2;
